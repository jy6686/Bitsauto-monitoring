# BUG-001 — Duplicate column-header mapping collision

- **Objective:** each vendor column maps independently even when headers repeat.
- **Scope:** `parseFile()` header handling (`server/routes-vendor-rates.ts`); no UI change.
- **Acceptance criteria:**
  - Sheet with duplicate headers (two "Rate") → each dropdown independent.
  - First occurrence keeps its name; duplicates → `_2`/`_3`; empties → `col_<i>`.
  - Import writes correct columns; row count matches Excel.
  - Real-file matrix passes (QuickComm/Telstra/Tata/BICS/HGC/OTEGlobe × correct
    sheet/header/dup-headers/dates/prefixes/row-count/margin) — see Bug Register.
- **Dependencies:** none.
- **Status:** **FIX READY** — do not merge until real-file matrix passes.
- **Owner:** Commercial.
- **Verification:** ✓ unit (`routes-vendor-rates-parse.test.ts`, 4/4); ☐ real files.
- **Related Bug(s):** BUG-002 (found during its test).
- **Related ADR:** [ADR-004](../../decisions/ADR-004-vendor-import.md).
- **Related VR:** none (implementation).
- **Branch/commits:** `fix/vendor-import-column-mapping` (`079ee08d`, `2ef1ba90`).
