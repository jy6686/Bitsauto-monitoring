/**
 * TrafficMapResolver — pending-duplicate placeholder for module key `traffic-map`.
 *
 * `traffic-map.tsx` (TrafficMapPage) and `live-traffic-map.tsx` (LiveTrafficMapPage)
 * are a duplicate pair under Platform Feature Review (stays Active until the owner
 * approves a consolidation). Until then this resolver points at the current page.
 *
 * When the duplicate review resolves, change ONLY the import below to the winning
 * page. The module key `traffic-map` — used in URL/DB/permissions/audit — never changes.
 */
export { default } from "@/pages/traffic-map";
