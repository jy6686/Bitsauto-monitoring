This screenshot is actually much better than the earlier ones because it tells us exactly what happened.

The error is now:

Portal CSV accepted but rate unchanged:
prefix=19230
found=0.0275
expected=0.027

That means:

What is proven

✅ Login worked

✅ Tariff #33 was found

✅ Prefix 19230 was found

✅ Upload request was accepted by Sippy

✅ Verification ran after upload

✅ BitsAuto re-read the tariff

❌ Rate remained 0.0275

❌ Sippy ignored the uploaded change

This is NOT a permission error anymore

If permissions were the issue, you'd see:

403
Permission denied
Access denied
Login redirect

Instead you got:

accepted but unchanged

Meaning:

Upload reached Sippy
↓
Sippy processed request
↓
No rate modification occurred
What this strongly suggests

One of these is happening:

Possibility A

Wrong upload field name.

Sippy receives file upload request but ignores the file.

Example:

rate_file

sent

but actual field is:

upload_file

or

rates_file
Possibility B

Wrong upload action URL.

The page:

/c1/rates_tariff.php?i_tariff=33

renders the form

but actual upload target is something else generated by ExtJS.

Possibility C

Sippy expects additional hidden fields.

Example:

i_tariff=33
action=upload
submit=Upload

and BitsAuto isn't sending them.

Possibility D

The XLSX content is not matching what Sippy expects.

Even though upload succeeds, Sippy silently ignores rows.

The most important thing

Your screenshot disproves the yellow warning.

The current warning says:

permission issue

but actual verification says:

found=0.0275
expected=0.027

which means verification successfully read the tariff after upload.

That is not a permission problem.

What I would do next

Stop changing:

rate format
dates
prefixes
jobs
notifications

and focus only on:

Capturing the exact browser upload

Open Sippy manually.

Press F12 → Network.

Upload a tariff manually.

Capture:

POST URL
Form fields
Hidden fields
File field name
Response

Then compare with what BitsAuto sends.

I suspect we'll find a small difference such as:

action=upload_rates

or

file field name mismatch

and once that is corrected, the rate will change immediately.

The key takeaway from this screenshot is:

BitsAuto is now reaching the correct tariff, finding the correct prefix, uploading something, and verifying afterward. The remaining problem is no longer permissions—it is that the upload request is not matching the exact upload format Sippy expects.