~/workspace$ grep -n "_dr.length" server/routes.ts

34732:      const _dr = Array.isArray(rows) ? rows : ((rows as any).rows ?? []); console.log("[dest] rows:", _dr.length, _dr[0] && JSON.stringify(_dr[0]).substring(0,100)); res.json(_dr);

35174:      const _dr = Array.isArray(rows) ? rows : ((rows as any).rows ?? []); console.log("[dest] rows:", _dr.length, _dr[0] && JSON.stringify(_dr[0]).substring(0,100)); res.json(_dr);

35267:      const _dr = Array.isArray(rows) ? rows : ((rows as any).rows ?? []); console.log("[dest] rows:", _dr.length, _dr[0] && JSON.stringify(_dr[0]).substring(0,100)); return res.json(_dr);

~/workspace$ grep -n -B15 "_dr.length" server/routes.ts | grep -E "app\.(get|post|put)|_dr\.length|executeSQL|db\.execute" | head -20

34732:      const _dr = Array.isArray(rows) ? rows : ((rows as any).rows ?? []); console.log("[dest] rows:", _dr.length, _dr[0] && JSON.stringify(_dr[0]).substring(0,100)); res.json(_dr);

35174:      const _dr = Array.isArray(rows) ? rows : ((rows as any).rows ?? []); console.log("[dest] rows:", _dr.length, _dr[0] && JSON.stringify(_dr[0]).substring(0,100)); res.json(_dr);

35258-  app.get('/api/destination-catalog/aliases/:destinationId', async (req: any, res: any) => {

35261-      const rows = await db.execute(sql`

35267:      const _dr = Array.isArray(rows) ? rows : ((rows as any).rows ?? []); console.log("[dest] rows:", _dr.length, _dr[0] && JSON.stringify(_dr[0]).substring(0,100)); return res.json(_dr);

~/workspace$ This actually exposes another issue.

The logging patch was applied to three different endpoints, but we still don't know whether the endpoint the UI uses is one of them.

From your grep:



34732
35174
35267

Only one of those is clearly:



/api/destination-catalog/aliases/:destinationId

The other two could be completely unrelated.

The browser error is still the key



TypeError: (ae ?? []).filter is not a function

That means React received a value that is not an array.

Not "an empty array."

Not "no data."

A different type entirely.

Let's identify the endpoint first

Run this:



sed -n '34700,34745p' server/routes.ts
echo "--------------------------------"
sed -n '35155,35185p' server/routes.ts

I want to see which routes correspond to:



34732
35174

Then inspect the frontend fetch

Run:



grep -n "product-registry/destinations" -n client/src/pages/destination-catalog.tsx
grep -n "destination-catalog" -n client/src/pages/destination-catalog.tsx

and then:



sed -n '1,220p' client/src/pages/destination-catalog.tsx

(or at least the section containing the useQuery).

I strongly suspect this pattern

Somewhere there is code like:



const { data = [] } = useQuery(...)

and then



data.filter(...)

But after the migration, the API is now returning something like:



{
    rows: [...]
}

or



{
    success: true,
    data: [...]
}

instead of



Destination[]

which produces exactly:



.filter is not a function

I would not modify any more backend code until we see the frontend.

You've already proven:





 Database ✅ 



 View ✅ 



 SQL query ✅ 



 Migration ✅ 

Now the evidence points squarely at the contract between the API and the React component. One look at the useQuery() implementation (or the Network response) should tell us exactly where the shape mismatch is occurring.