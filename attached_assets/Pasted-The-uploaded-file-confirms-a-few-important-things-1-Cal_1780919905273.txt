The uploaded file confirms a few important things:

1. Call Governance Auto-Update Issue

This is not primarily a polling problem.

According to the analysis in the file:

Live tab already refreshes every 3 seconds
Billing tab refreshes every 10 seconds when pending CDRs exist

The real issue is:

Server restart
      ↓
Governed calls already cut
      ↓
CDR lookup timers lost
      ↓
Calls remain "No CDR"
      ↓
UI keeps polling stale records forever
Recommended Permanent Fix

On startup:

Load governed_calls
where:

cdrStatus IS NULL
AND
startTime > NOW() - 45 minutes

Re-run CDR lookup

This automatically recovers calls after deployments/restarts.

2. 2060 vs 20601

The file strongly suggests:

Most Likely Cause

Inside Sippy:

Gateway
   ↓
Tech Prefix

or

Routing Group
   ↓
Dial Prefix

currently set to:

2060

instead of:

20601
Verify

During an active call:

asterisk -rx "core show channels verbose"

Check:

PJSIP channel callerid

If Asterisk already receives:

2060923xxxx

then Asterisk is innocent.

The problem is upstream in Sippy.

3. Vendor Cost

The file confirms what we've been discussing.

Current chain:

Customer Rate

exists.

Missing:

Vendor Rate

Therefore:

True Margin

cannot be calculated reliably.

Recommended Architecture
Vendor Rate Cards
      ↓
Prefix Matching
      ↓
Per Call Cost
      ↓
Margin Engine

Table:

vendor_rate_cards

vendor_id
prefix
rate
effective_date

This should become the foundation of:

Rate Manager
Pricing Intelligence
Vendor Impact Analysis
Margin Truth
My Priority Order
Immediate

Fix:

Startup CDR Backfill

because it affects operations daily.

Then

Fix:

2060 → 20601

by checking:

Sippy Gateway Prefix
Routing Group Prefix

before touching Asterisk.

Then

Build:

Vendor Cost Repository

because that unlocks:

True Margin
Vendor Impact
Pricing Intelligence
Rate Management
One Observation

The file indicates the governance module has been worked on repeatedly for several days.

At this point I would stop making small fixes and perform a full call trace:

Client
   ↓
Sippy
   ↓
Asterisk
   ↓
Vendor

capturing:

INVITE
From
To
R-URI
CallerID
Destination

at every hop.

That will identify conclusively whether:

20601

is being changed by:

Sippy

or

Asterisk

instead of continuing trial-and-error fixes.

My suspicion from the uploaded analysis is 80–90% that the prefix is being generated by Sippy configuration, not by Asterisk.