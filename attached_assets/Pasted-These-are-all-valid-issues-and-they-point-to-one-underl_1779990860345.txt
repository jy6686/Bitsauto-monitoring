These are all valid issues — and they point to one underlying problem:

the finance layer was built correctly architecturally,

but the operational data binding and UX composition still need stabilization.

Here’s the proper correction plan.

1. Invoice Generation Should Use CLIENT NAME

(not tariff name)

Current Problem

Invoices are likely grouped/generated by:

i_tariff

instead of:

account/client identity

That is wrong operationally.

Tariff is:

rating logic,
pricing structure.

Invoice ownership must always be:

client account.
Correct Invoice Hierarchy
Client Account
    ↓
Billing Period
    ↓
CDRs / Snapshots
    ↓
Tariff Resolution
    ↓
Invoice

NOT:

Tariff
    ↓
Invoice
Correct Source

Fetch from:

Sippy account name

Primary key:

i_account,
account_name.

Tariff should only affect:

rating,
margin,
reproduction.

NOT invoice grouping.

2. DMR Not Creating Reports

This is likely:

aggregation mismatch,
Sippy summary parsing issue,
account resolution issue,
date-window mismatch.

Most probable issue:

DMR generation currently depends on incomplete account mapping

instead of:

direct Sippy account normalization
Correct DMR Logic

DMR should ALWAYS:

fetch Sippy account name first,
normalize by account_id,
aggregate by:
account,
vendor,
destination,
billing day.

Then:

DMR = operational truth layer

Everything else derives from it.

3. Duplicate Sidebar Menus

This is important:
you already decided:

top navigation governance

instead of:

huge sidebar tree

Correct decision.

What Should Happen
Sidebar should become:
contextual module list only.

Minimal.

Top Navigation Should Become
Portal
  ↓
Section Tabs
  ↓
Context Sidebar

Meaning:

Top Bar
KAM
NOC
Finance
Admin

Then:

Clients
Commercial
Billing
Revenue Assurance

Then sidebar:

actual modules.

Correct architecture.

REMOVE

Duplicate:

finance items,
reports,
invoices,
reconciliation

from:

multiple sidebar groups

This is causing:

navigation pollution.
4. Reconciliation Not Fetching Client Name from Sippy

This is critical.

The reconciliation engine should NEVER rely on:

manually entered names
Correct Flow
Sippy Account
    ↓
Normalized Client Identity
    ↓
DMR
    ↓
Invoice
    ↓
Reconciliation

Everything must use:

i_account,
account_name,
external_account_ref.
Add Immediately
client_identity_map
i_account
client_name
display_name
billing_name
crm_name
portal_name

This becomes:

identity normalization layer.

Very important long-term.

5. Fetch Default Account Name from Sippy Everywhere

Correct.
This should become:

platform-wide rule.
Platform Standard

Every:

report,
DMR,
invoice,
dispute,
reconciliation,
dashboard,
AI alert

should default to:

Sippy account_name

NOT:

tariff,
vendor,
internal IDs,
temporary labels.
6. Finance Billing Group Not Functional Properly

This is because:
the Finance Portal still exists as:

many pages,

not:

one finance operational workspace.

Exactly the issue discussed earlier.

Correct Finance Structure
Finance Portal
Billing
invoices
invoice jobs
templates
credit notes
Revenue Assurance
DMR
reconciliation
rating verification
snapshots
Collections
disputes
credit control
collections
Governance
AI assurance
audit
approvals
adjustments
MOST IMPORTANT FIX FIRST
Identity Normalization Layer

Before ANY more finance work.

Because:

invoices,
DMR,
reconciliation,
disputes,
reports

all depend on:

consistent client identity.

Without this:
everything fragments.

Recommended Immediate Sprint
T001 — Client Identity Normalization

(highest priority)

T002 — DMR Stabilization

(make operational truth reliable)

T003 — Invoice Generation Refactor

(client-based billing)

T004 — Finance Navigation Cleanup

(remove sidebar duplication)

T005 — Finance Cockpit Composition

(unified workspace)

This is the correct stabilization sprint before more feature expansion.