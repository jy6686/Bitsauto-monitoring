This is the correct conclusion.
Do NOT rebuild the client portal from scratch.

You already have a functioning foundation:

token-based scoped access
account isolation
client-safe endpoints
usage visibility
billing visibility
CSV exports

That is a major head start.

✅ Best Strategic Decision
Extend the existing portal architecture

NOT:

build an entirely separate auth + client framework

The current token model is actually very smart for telecom operations because:

Benefit	Why it matters
No password management	Less support burden
Fast provisioning	Send link instantly
Account-scoped by design	Safer
Revocable	Operationally clean
Expiry supported	Enterprise-friendly
White-label compatible	Excellent for resellers

This is very similar to how many wholesale telecom portals actually work.

✅ Recommended Direction NOW
Keep:
/portal/:token
/api/portal/*

Do NOT pause momentum to redesign auth.

✅ What should happen next (recommended order)
Phase 1 — Finish Existing Portal Properly

This is the fastest path to a strong V1.

1. Upgrade Overview Tab

Currently:

Total Calls
Connected Calls
ASR
Duration

Add:

ACD
PDD
Failed Calls
Successful Calls
Last Refresh Time
2. Add Destinations Tab

This is the biggest missing operational value.

Table:

| Destination | Calls | Minutes | ASR | ACD |

Filters:

Today
7d
30d

This is extremely valuable for clients.

3. Improve Billing Tab

Current estimated billing is good enough for V1.

Add:

downloadable statement CSV
daily totals
invoice placeholder (“Coming Soon”)

Do NOT implement:

payments
invoice generation
gateway integrations

yet.

4. Harden Token Management

Very important operationally.

Add:

Feature	Value
Copy Link button	Huge usability win
Last Used timestamp	Security visibility
Expiry badge	Operational clarity
Revoke confirmation	Safety
Account name prominently shown	Prevent mistakes
5. Add Read-only Balance Card

Show:

current balance
credit limit
overdue warning
last payment date

This completes the financial visibility layer.

Phase 2 — Ticket System (V1.1)

This should come AFTER the portal stabilization.

Because tickets require:

DB schema
attachments
notifications
operator inbox
lifecycle management

This is materially larger than the other enhancements.

🚨 Most important architectural recommendation

Do NOT expose raw CDR complexity.

The portal should transform telecom mechanics into:

business-readable visibility

Example:

GOOD
Destination: Pakistan Mobile
Calls: 4,812
ASR: 43%
BAD
Vendor: XYZ Telecom
Route Group: Pak-Mobile-LCR
Connection: PK-01

That boundary must remain strict.

✅ Strong Recommendation About UI

Keep the portal:

lightweight
fast
table-oriented
mobile-friendly

Avoid:

complex charts
AI widgets
operational terminology
engineering panels

The simpler the client experience:

the more enterprise-ready it feels
the lower the support burden
✅ Final Recommended Immediate Worklist
Next 5 implementations
Priority	Feature
1	Add ACD + PDD + failed/successful to Overview
2	Add Destinations tab
3	Improve token management UX
4	Add read-only balance enhancements
5	Ticket system (after stabilization)

That path gets you to a polished, deployable Client Portal V1 very quickly without destabilizing the platform architecture you already built.

3. Improve Billing Tab

Current estimated billing is good enough for V1.

Add:

downloadable statement CSV/Excel, only off peak time: 22:00 GMT:00
daily totals
invoice placeholder (“Coming Soon”)

Do NOT implement:

payments
invoice generation
gateway integrations

yet.
5. Add Read-only Balance Card

Show:

current balance
credit limit
overdue warning
last payment date
approved :IPs
Prefix?CLD RULE: *4digit* alredy assined when creating new account 



This scope is clean, realistic, and aligned with the “client-safe telecom analytics portal” direction. I would implement it exactly this way — extending the existing /portal/:token system rather than rebuilding the portal architecture.

Approved V1.1 Additions
1. Billing Tab Improvements

Keep the current estimated billing logic, but upgrade presentation and export usability.

Add
Download Statement CSV
Download Statement Excel
Daily totals summary table
“Invoices Coming Soon” placeholder card
Export restriction window:
Allow exports only during off-peak window:
22:00–06:00 GMT+0
Do NOT add yet
Payment gateway
Invoice PDF generation
Auto collection
Stripe/JazzCash/EasyPaisa integrations
Recharge system

This keeps the portal operationally safe and avoids billing-closure complexity.

2. Read-Only Balance Card
Show
Current Balance
Credit Limit
Last Payment Date
Overdue Warning badge
Approved IPs
Assigned CLD Prefix (4-digit rule)
Do NOT show
Manual adjustments
Margin
Vendor cost
Internal billing notes
Routing limits
Credit operations
Recommended UI Structure
Overview Tab

Top cards:

Card	Value
Current Balance	USD
Credit Limit	USD
ASR	%
ACD	mm:ss
Active Calls	live
Calls Today	total

Second section:

Approved IPs
Assigned CLD Prefix
Last Payment Date
Account Status
Billing Tab
Sections
Daily Usage Totals

| Date | Calls | Minutes | Estimated Cost |

Export Actions
Download CSV
Download Excel
Invoice Placeholder
Invoices & Online Payments
Coming Soon
Important Architecture Rule

The 4-digit CLD rule should remain informational only inside the portal.

Clients may VIEW:

Assigned CLI/CLD policy
Active prefixes

Clients must NOT:

Edit prefixes
Create routing rules
Change normalization logic
See routing transformations

That stays operator-only forever.

My Recommendation Before Implementation

Implement in this order:

Priority	Item	Effort
1	Balance Card expansion	Low
2	Billing export CSV/Excel	Medium
3	Daily totals table	Low
4	Off-peak export restriction	Low
5	Invoice placeholder	Very Low

Do NOT start tickets yet unless the internal operator inbox workflow is finalized first.

One Important Suggestion

For Approved IPs:
show masked formatting by default:

Example:

203.0.113.xxx
198.51.100.xxx

Then allow:
“Show Full IPs”

This avoids accidental exposure during screen sharing or screenshots while still remaining operationally useful.

Final Architectural Direction

Your client portal is now correctly positioned as:

Enterprise telecom visibility layer

NOT:

Carrier operations system

That separation is the right long-term product decision.