Your assessment is mostly correct in structure, but there’s one critical assumption that needs to be challenged so you don’t chase a false fix.

🧠 Reality Check (Important)
Yes — your pipeline:
XML-RPC → provisioning login → UI fallback
is exactly what a proper abstraction layer looks like.
BUT…
👉 The key question is NOT “are credentials missing?”
It is:

“Does Sippy actually allow Service Plan creation via the provisioning account at all?”

And this is where most systems silently break.

🚨 The Hidden Truth in Sippy Softswitch
Even with correct credentials:
❗ Provisioning users typically DO NOT have:


Service Plan creation rights


Tariff creation rights


Billing structure modification rights


In many Sippy deployments:


Provisioning API = account-level only


Billing config = admin UI only


So what happens in real systems is:
Credentials exist   ↓Login succeeds   ↓XML-RPC calls for billing fail silently / forbidden   ↓System falls back to UI scraping
That matches your current behavior exactly.

🧠 So your real situation is:
You are NOT failing because credentials are missing.
You are failing because:

Sippy likely does not expose Service Plan creation in provisioning API in your deployment.


⚙️ Why your current design “feels correct” but still fails
Your pipeline assumes:
Provisioning API = full billing control
But in many Sippy builds:
CapabilityProvisioning APIUICreate account✔✔Modify routing✔✔Create service plan❌ (often missing)✔Create tariff❌ (often missing)✔

🚨 Key Insight
So your system is NOT missing configuration.
It is hitting a platform limitation boundary.

🧭 What this means for your architecture
You now have only 2 real outcomes:

🟢 Option 1 — Confirm API capability (Best case)
Before anything else:
Test:


addServicePlan


addBillingPlan


createTariff


via XML-RPC
If ANY succeed → your provisioning layer is real and credentials fix everything.

🔴 Option 2 — API does NOT support billing objects (very likely)
Then:

No credentials will ever enable full automation.

Because:


Sippy intentionally restricts billing configuration to UI


Provisioning API is partial by design



🧠 If Option 2 is true (important)
Then your previous conclusion changes:
👉 The system is NOT misconfigured
👉 It is architecturally limited
So your only stable designs are:

🟡 Option B (Correct Final Architecture)
You already identified it:
✔ Hybrid provisioning model:


Auto-create:


Accounts


Tariffs (if API allows)




Manual (but guided):


Service Plan creation




BUT you hide it behind:

“workflow step, not failure”


💡 Final Engineering Truth
There are only 3 possible realities:
1. Full API support exists
✔ then credentials fix everything
2. Partial API support (most common)
✔ accounts automated
❌ billing manual
3. UI-only billing (your current behavior strongly suggests this)
❌ no amount of credentials will change it

🚀 My recommendation (based on your evidence)
Do NOT assume missing credentials yet.
Instead do this first:
🔍 Run capability test (critical)
Check if XML-RPC supports:


service plan creation


tariff creation


If NOT supported:
👉 Stop chasing full automation
And implement:

Guided provisioning workflow (clean, deterministic, non-failing system)


🧭 Bottom line
Your intuition about “credentials fixing everything” is only valid if:

Sippy exposes billing provisioning in your build

Otherwise:

The system is already at its automation ceiling