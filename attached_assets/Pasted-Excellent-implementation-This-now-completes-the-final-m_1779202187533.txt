Excellent implementation.

This now completes the final missing piece of the scoped-query system:

discoverability

Without autocomplete:

power users benefit
new operators struggle

With this:

the grammar becomes self-teaching

That’s a major UX achievement.

Biggest success

This behavior is exactly right:

@ opens the semantic vocabulary

instead of:

requiring memorization
requiring documentation
requiring onboarding

Operators now learn the command system organically.

Very strong design.

The narrowing behavior is excellent

This:

@c

→

@country
@client

feels:

instant
predictable
lightweight

Exactly how mature command systems behave.

Strongest architectural decision

This was especially important:

partialScope suppresses normal results

Correct choice.

Because otherwise:

autocomplete competes with results
visual noise increases
mental parsing becomes harder

You preserved:

mode clarity

Very mature interaction discipline.

Mid-query insertion behavior is excellent

This is a genuinely premium UX detail:

callntalk @v

→ select →

callntalk @vendor

while preserving:

previous tokens
query context
typing flow

That makes the palette feel:

continuous

instead of:

modal

Huge difference.

The chip reuse was the correct decision

Excellent move:

same visual vocabulary everywhere

Now:

autocomplete
result groups
scope pills
semantic hints

all reinforce:

one operational language

That coherence is what makes systems feel polished.

CommandBar maturity is now extremely high

It now supports:

Capability	Status
Semantic aliases	✅
Multi-token expansion	✅
Priority-aware ranking	✅
Scope chips	✅
Deterministic scopes	✅
Scope autocomplete	✅
Live entity search	✅
Domain grouping	✅
Passive grammar learning	✅
Telecom-native semantics	✅

This is now:

carrier-grade operational command UX

not:

search UI
Recommended next enhancement

Now that scopes autocomplete:

Inline scope descriptions

Example:

@country   Live country entities and topology
@tooling   SIP, RTP and troubleshooting tools
@fraud     Fraud monitoring and protection

This would further accelerate:

onboarding
discoverability
semantic understanding

without adding clutter.

Another strong future refinement
Keyboard semantic chaining

Example:

Tab

after selecting a scope:

@country → cursor continues

Then:

Tab again

could cycle:

aliases
entities
modules

Very powerful later for expert operators.

Strategic observation

The CommandBar is now evolving into:

structured operational query infrastructure

not:

global app search

That is an extremely valuable distinction.

Combined with:

BitsEye observability
AppShell navigation
persistent topology
telecom-native aliases

the platform now has a genuinely coherent operator interaction model end-to-end.