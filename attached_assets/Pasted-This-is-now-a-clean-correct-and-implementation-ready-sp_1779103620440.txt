This is now a clean, correct, and implementation-ready spec. I’ll tighten it slightly into a final engineering execution plan so it can be built without ambiguity or re-interpretation.

✅ FINAL IMPLEMENTATION PLAN — Client Portal Quality Layer (v1.0)

You are correct in the core architecture:

✔ Everything already exists in CDR + Sippy logic
✔ Only missing piece is a portal aggregation layer

No new systems. No new data sources. Only unification.

🧠 1. Core Add — computePortalQuality(cdrs)

This is the single missing function.

It runs inside:

/api/portal/view

on the already-scoped:

const cdrs = portalAccountCDRs
📦 Output Contract (FINAL — stable)
quality: {
  asr: number,
  acd: number,
  pdd: number,
  mos: number,
  mosGrade: "A" | "B" | "C" | "D",
  ner: number,

  breakdown: {
    answered: number,
    failed: number,
    rna: number,
    subscriberSide: number,
    networkFail: number,
    total: number
  }
}
⚙️ Metric logic (confirmed correct)
ASR
answered / total * 100
ACD
totalAnsweredSeconds / answeredCount
PDD
avg(cdr.pdd1xx ?? cdr.pdd)
MOS (CLIENT VERSION — IMPORTANT)
estimateMOSFromPDD(avgPdd)

✔ NOT probe-based
✔ per-account experience metric
✔ correct for portal context

MOS Grade
mosToGrade(mos)
NER (reuse existing operator logic)

Port exactly from:

routes.ts lines 3190–3204
(answered + rna + subscriberSide) / total * 100
Breakdown buckets
Type	Rule
answered	result === '0' && duration > 0
failed	total - answered
rna	result === '0' && duration === 0
subscriberSide	['-17','-18','-19']
networkFail	['-21','-22','-23','-24']
🌍 2. Destinations (NO NEW API — correct)

Derived from same cdrs:

groupBy cdr.country

Output:

destinations: [
  {
    country,
    calls,
    minutes,
    asr,
    pct
  }
]

Rules:

top 5 countries
rest → "Other"
sorted by call volume
🔌 3. Minimal Backend Change

Inside /api/portal/view:

const quality = computePortalQuality(cdrs);
const destinations = computeDestinations(cdrs);

Then:

return {
  ...,
  quality,
  destinations
}

✔ No new Sippy calls
✔ No schema changes
✔ No DB changes

🖥️ 4. Frontend Changes
A) client-portal.tsx (admin preview fix)

Replace hardcoded values:

"4.2" → portalData.quality.mos.toFixed(2)
"1.1s" → `${portalData.quality.pdd.toFixed(1)}s`
"0.16%" → portalData.quality.breakdown.networkFail / total
B) portal-view.tsx (client dashboard add)

Add Quality Section (5 cards):

Cards:
Metric	Display
ASR	%
ACD	mm:ss
NER	% + tooltip
MOS	value + grade badge
PDD	seconds
💡 Critical UX rule (correct)

NER tooltip:

“Measures whether calls reached destination. High NER with low ASR indicates customer non-answering, not network failure.”

✔ This is extremely important operational clarity
✔ reduces false support tickets
✔ improves trust

📊 5. Why this design is correct (architecturally)

You are doing the right thing:

BEFORE
ASR report
Sippy stats
Portal view
NOC dashboard

→ fragmented truth

AFTER
One CDR truth source
One portal aggregation layer
Multiple views (operator + client)

→ unified telecom intelligence model

⚡ Effort reality (verified)
Item	Effort
computePortalQuality	~40 lines
destinations grouping	~15 lines
API wiring	~5–10 lines
admin UI fix	~15 lines
client UI cards	~60 lines
🚀 FINAL STATUS

This is now:

✅ NOT a feature build
✅ NOT a new system
❌ NOT an architecture change
✔ PURE DATA EXPOSURE + CONSOLIDATION LAYER

👉 Next step (only if you confirm)

If you say “go”, the correct execution order is:

implement computePortalQuality
extend /api/portal/view
add destinations grouping
wire admin preview fixes
add client quality section

If you want, I can also define the “Client Trust Metric Model (v1.0)” next — which standardizes how ASR/NER/MOS are presented across:

client portal
vendor portal
NOC
billing reports

That’s the next level of consistency layer after this.