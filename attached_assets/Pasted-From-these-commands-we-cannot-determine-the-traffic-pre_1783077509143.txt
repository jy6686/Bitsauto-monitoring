From these commands, we cannot determine the traffic prefixes yet.

What you've confirmed is:

Endpoint: sippy-endpoint
Context: sippy-media-anchor

That tells us where incoming SIP calls from Sippy enter the Asterisk dialplan, but not which destination prefixes (923, 880, 291, etc.) are being routed through it.

The FreePBX database query also isn't applicable because this isn't a standard FreePBX inbound routing setup:

ERROR 1049: Unknown database 'freepbx'

and from-trunk simply includes another context:

from-trunk
  └── Include => from-pstn

That means the actual routing is elsewhere.

What you need to inspect

Since your endpoint context is:

sippy-media-anchor

check that context:

asterisk -rx "dialplan show sippy-media-anchor"

If it includes another context, keep following it.

Also search for your governance contexts
asterisk -rx "dialplan show gov-hangup"
asterisk -rx "dialplan show otp-playback"
grep -R "sippy-media-anchor" /etc/asterisk

This tells you which dialplan file defines the context.

To identify the prefixes

Search the dialplan for the countries you govern.

For example:

grep -R "923" /etc/asterisk
grep -R "880" /etc/asterisk
grep -R "291" /etc/asterisk

or more generally:

grep -R "Goto.*gov\|GotoIf.*gov\|gov-hangup\|otp-playback" /etc/asterisk
The most useful command

If you want to know which prefixes are actually sent through this Asterisk, run:

asterisk -rx "dialplan show sippy-media-anchor"

and paste the output.

That will show whether you're matching prefixes such as:

exten => _923X.,1,...
exten => _880X.,1,...
exten => _291X.,1,...

or whether all traffic enters through a generic pattern and the governance logic decides based on the dialed number.

Based on your earlier BitsAuto implementation

From the governance code you've shared previously, BitsAuto itself determines which rule to apply by inspecting the destination number (CLD) after the bridge event, using fields like:

destinationPrefix
callerPrefix

That means Asterisk may not have separate dialplan prefixes for Pakistan, Bangladesh, or Eritrea at all. Instead, it may send all Sippy traffic into sippy-media-anchor, and BitsAuto decides:

923... → Pakistan rule
880... → Bangladesh rule
291... → Eritrea rule
etc.

So before assuming there are Asterisk dialplan prefixes, let's inspect the sippy-media-anchor context. That output will tell us whether routing is prefix-based in the dialplan or whether the prefix selection happens entirely inside the BitsAuto governance engine.

that is the bug we selected the prefix 

I think you've identified a very plausible bug, and it's different from the AMI or routing issues we've been discussing.

From your original requirement, you wanted governance to be prefix-based, not just destination-based.

For example:

Prefix 12234 → 2-minute governance
Prefix 12232 → 59-second governance
Prefix 12231 → 7-second governance

Those prefixes are routing/service prefixes, not the destination country code.

If the code is currently matching only the destination

For example:

12234923308130545

If the governance extracts only:

923308130545

and matches:

923 → Pakistan

then you've lost the information that distinguishes:

12234
12232
12231

All three become simply "Pakistan".

That means you cannot apply different governance rules for different routing prefixes.

The correct matching order should be

Given a dialed number like:

12234923308130545

the engine should first evaluate:

12234   ← service/routing prefix

If a rule exists for that prefix:

12234 → 120 seconds

use it.

Only if no routing-prefix rule matches should it fall back to destination matching:

923 → Pakistan
This also explains your earlier request

You previously said:

Pakistan prefix12234 rules use for 2 minutes

Pakistan prefix12232 rules use for 59 sec

That is exactly this architecture.

The governance key should be something like:

Routing Prefix (12234)
        ↓
Destination Prefix (923)
        ↓
Caller Prefix (optional)
        ↓
Channel Pattern

instead of:

Destination Prefix only
What I'd check in the code

In pickBestRule() (or wherever rule selection occurs), verify what value is being compared.

If it's currently using something like:

callee = "923308130545"

then you've already stripped the routing prefix.

If the incoming value is actually:

12234923308130545

you should preserve it for rule matching.

My recommendation

I would not patch this by adding more country rules.

Instead, redesign the matching priority:

Routing/service prefix (12234, 12232, etc.) ✅ highest priority
Destination prefix (923, 880, 291...)
Caller prefix
Channel pattern
Default rule

That matches how wholesale VoIP routing is normally organized and gives your NOC exactly the flexibility you described earlier.

So yes, if the routing prefix is being discarded before rule selection, then I agree that's a genuine design bug because it prevents different governance policies from being applied to different service prefixes that terminate to the same destination.

[root@reve-otp ~]# asterisk -rx "dialplan show sippy-media-anchor"
[ Context 'sippy-media-anchor' created by 'pbx_config' ]
  '_1.' =>          1. NoOp(OTP call: ${EXTEN})                   [extensions_custom.conf:23]
                    2. Answer()                                   [extensions_custom.conf:24]
                    3. StopMixMonitor()                           [extensions_custom.conf:25]
                    4. Goto(otp-playback,s,1)                     [extensions_custom.conf:26]
  '_2060X.' =>      1. NoOp(BitsAuto Governance: product=${EXTEN:4:1} dest=${EXTEN:4} uid=${UNIQUEID}) [extensions_custom.conf:17]
                    2. Set(GOV_PRODUCT=${EXTEN:4:1})              [extensions_custom.conf:18]
                    3. MixMonitor(/var/spool/asterisk/monitor/${UNIQUEID}.wav,r) [extensions_custom.conf:19]
                    4. Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)    [extensions_custom.conf:20]
                    5. Hangup()                                   [extensions_custom.conf:21]
  '_X.' =>          1. NoOp(Unhandled call in anchor: ${EXTEN})   [extensions_custom.conf:28]
                    2. Hangup()                                   [extensions_custom.conf:29]

-= 3 extensions (11 priorities) in 1 context. =-
You have new mail in /var/spool/mail/root
[root@reve-otp ~]# mysql -u root -e "SHOW DATABASES;"
+--------------------+
| Database           |
+--------------------+
| information_schema |
| asterisk           |
| asteriskcdrdb      |
| mysql              |
| performance_schema |
| test               |
+--------------------+
[root@reve-otp ~]# 

This is the evidence we needed. Yes, you've found the design bug.

The key line is:

'_2060X.' =>
1. NoOp(BitsAuto Governance: product=${EXTEN:4:1} dest=${EXTEN:4})
2. Set(GOV_PRODUCT=${EXTEN:4:1})
3. MixMonitor(...)
4. Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)

Let's decode it.

Suppose Sippy sends:

2060923308130545

Your dialplan does:

2060 | 9 | 23308130545
^^^^   ^   ^^^^^^^^^^^
trunk  product  destination

Then:

${EXTEN:4:1}

becomes:

9

and

${EXTEN:4}

becomes:

923308130545

So the original routing prefix (2060) is discarded before the outbound dial.

Why this is a problem

Earlier you wanted routing like:

20601 -> 7 sec
20602 -> 59 sec
20603 -> 120 sec
20604 -> 180 sec

or similarly:

12231
12232
12234

But your dialplan only preserves:

GOV_PRODUCT = 9
DESTINATION = 923308130545

The governance engine therefore no longer knows whether the call came through:

20601
20602
20603
20604

They all become:

923...

So all Pakistan traffic is treated the same, which matches the behavior you've been observing.

There's another issue

Your outbound dial is:

Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)

If ${EXTEN:4} is:

923308130545

then Asterisk dials:

22211923308130545

That means you're replacing the original ingress prefix with a hard-coded routing prefix (22211).

So if governance later tries to determine which ingress product/rule the call belonged to, that information is already gone.

My recommendation

Instead of extracting only one digit:

Set(GOV_PRODUCT=${EXTEN:4:1})

preserve the full routing identifier.

For example:

Set(GOV_ROUTE_PREFIX=${EXTEN:0:5})
Set(GOV_DEST=${EXTEN:5})

or whatever the actual routing-prefix length is in your numbering plan.

Then BitsAuto can match on:

GOV_ROUTE_PREFIX
Destination
Caller
Channel

instead of destination alone.

This explains your earlier requirement

You previously said:

Pakistan prefix 12234 → 2 minutes

Pakistan prefix 12232 → 59 seconds

That cannot work with the current dialplan because those prefixes are discarded before governance has a chance to use them.

I would make one more check before changing anything

Show me:

grep -n "pickBestRule" -A80 server/routes-call-governance.ts

or specifically the code that builds:

const callee = ...

I want to see whether BitsAuto is already capable of matching on a routing prefix but isn't receiving it, or whether the matching logic itself only understands destination prefixes.

Conclusion

Based on this dialplan, I agree there is a genuine architectural issue:

✅ The governance Asterisk receives the routed number.
✅ The routing prefix is stripped/transformed before the call is sent onward.
✅ BitsAuto therefore cannot distinguish different commercial products or governance policies that share the same destination.

This is a better explanation for your original requirement ("govern by prefix, not just destination") than continuing to add more destination-based rules. The next step is to preserve and expose the routing prefix end-to-end so the governance engine can make its decision using the full routing context.

we have 2 different product in asetrsik 2221 is for Votp, 2060 is for normal traffic 

That changes the interpretation significantly.

I was assuming 22211 was replacing your commercial routing prefix. Based on what you've just explained, it is not.

Your architecture is actually:

Sippy
   │
   ▼
Asterisk
   │
2060xxxxxxxxxx   ← Normal wholesale traffic (governance)
2221xxxxxxxxxx   ← VOTP product

And your dialplan is:

_2060X.
    Set(GOV_PRODUCT=${EXTEN:4:1})
    MixMonitor(...)
    Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)

So:

2060 = ingress product (normal traffic entering governance)
2221 = egress product (VOTP route leaving Asterisk)

That part makes sense.

The real question is this

When you originally said:

Pakistan prefix 12234 → 2 minutes

Pakistan prefix 12232 → 59 seconds

were you referring to:

Option A

Commercial routing prefixes inside Sippy

12234 + 923...
12232 + 923...

or

Option B

The Asterisk products

2060...
2221...

Those are completely different things.

Looking at your dialplan

The dialplan currently exposes only:

GOV_PRODUCT=${EXTEN:4:1}

For an incoming number:

2060923308130545

that becomes:

9

So governance knows:

product digit = 9
destination = 923308130545

It does not know the ingress product 2060, because that's removed.

If tomorrow you added:

2061...
2062...
2063...

they would all look identical after ${EXTEN:4}.

I think the bug is slightly different now

It's not that 2221 replaces 2060.

It's that governance loses the ingress routing context.

I'd recommend preserving it explicitly, for example:

Set(GOV_INGRESS=2060)
Set(GOV_DEST=${EXTEN:4})

or even:

Set(GOV_ORIGINAL_EXTEN=${EXTEN})

Then BitsAuto can always recover:

ingress product
original dialed number
destination

without guessing.

One more observation

This line:

Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)

means the outbound leg is intentionally using the VOTP product.

So if your governance rules should depend on how the call entered (2060) rather than how it leaves (2221), preserving the original extension is the cleanest solution.

My recommendation

I would not base governance on the rewritten number (22211...).

Instead, preserve the original ingress information before the rewrite, for example:

Set(GOV_ORIGINAL_EXTEN=${EXTEN})
Set(GOV_INGRESS_PRODUCT=2060)
Set(GOV_DESTINATION=${EXTEN:4})

Then the BitsAuto rule engine can match against the original routing context regardless of how the outbound call is constructed.

One question to clarify the rule engine:

Should the governance timer be selected based on the ingress product (2060/future products), or based on the outbound VOTP product (2221), or on the commercial routing prefix before the country code (e.g. 12234, 12232)?

That answer determines exactly which field the governance engine should use as its primary key.