This output is actually very useful. It confirms a few more things.

What this proves
1. chan_sip is the active trunk to Sippy

Your outbound dialplan uses:

Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)

and your SIP peer is:

Name      : sippy
Context   : sippy-media-anchor
ToHost    : 191.101.30.107
Addr->IP  : 191.101.30.107:5060
Status    : OK (207 ms)

So the peer is healthy.

2. The peer is reachable
Status : OK (207 ms)

means SIP OPTIONS qualify is succeeding.

Therefore:

✅ Network connectivity exists.
✅ UDP communication exists.
✅ The peer is responding.
3. The context is correct
Context : sippy-media-anchor

So any incoming INVITE from this SIP peer should land in

sippy-media-anchor

which is exactly where your governance logic lives.

This narrows the problem considerably

We have now verified:

✅ SIP peer exists.
✅ SIP peer is alive.
✅ Context is correct.
✅ Dialplan exists.
✅ Pattern _2060X. matches.
✅ Port 5060 is listening.

None of those are the issue.

What we still have not observed

We have not yet seen a live INVITE arriving.

That's the missing piece.

Until we see:

Received SIP INVITE

followed by

Executing [2060...@sippy-media-anchor]

we can't say whether the problem is:

before the dialplan,
inside the dialplan,
or after the dialplan (AMI/governance).
One thing I also noticed

Your system is using both SIP stacks:

chan_sip (legacy)
PJSIP

Your dialplan shows:

Dial(SIP/sippy/...)

while earlier we saw a PJSIP endpoint named sippy-endpoint.

That's not inherently wrong—many deployments run both stacks—but it means you should verify that Sippy is consistently using the intended stack for inbound and outbound traffic.

I would not change anything else right now

Given that Sippy is currently performing work, I would avoid making any configuration changes.

When it's finished, the first live test should be:

Enable:

pjsip set logger on

and, because you're also using chan_sip:

sip set debug peer sippy
Place one controlled OTP call.
Capture the Asterisk CLI output from the moment the call starts until it ends.

That single trace will show whether the INVITE reaches sippy-media-anchor, whether MixMonitor starts, whether Dial(SIP/sippy/...) executes, and whether the call bridges. Once we have that, we can identify the exact failure point instead of continuing to eliminate possibilities.