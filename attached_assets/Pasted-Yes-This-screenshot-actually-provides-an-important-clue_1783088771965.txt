Yes. This screenshot actually provides an important clue.

What the ASR/ACD report tells us

For the last hour:

Vendor/Connection	Calls	Billable	ACD
Callntalk	6,402	1,131	00:13
asterisk(in)/asterisk-in	400	0	00:00

So:

Sippy IS routing traffic to asterisk(in)
It is not bypassing Asterisk
400 calls have reached this vendor connection in the last hour.

This is different from what we initially suspected.

Now combine it with the Vendor CDR

Earlier you showed Vendor CDR:

Outgoing CLD
20608801319653537

Remote IP
159.223.32.59

Result
Timeout (100)

Duration
00:00

That proves Sippy is sending the call to:

159.223.32.59

which is your Asterisk.

But Asterisk shows no INVITEs

This is the contradiction.

Sippy reports:

400 calls

Asterisk reports:

OPTIONS
OPTIONS
OPTIONS

No INVITEs.

These two facts cannot both be true unless one of these is happening.

Possibility 1 (most likely)

The 400 calls are failing before Asterisk answers them.

For example:

Sippy
   |
INVITE
   |
Asterisk immediately rejects

If this happens, Sippy counts the attempt.

Asterisk never creates a bridge.

BitsAuto never receives BridgeEnter.

Governance remains empty.

Possibility 2

The INVITEs are arriving on a different SIP stack.

Notice your server has both:

chan_sip

and

PJSIP

loaded.

Earlier we saw:

PJSIP endpoint:
sippy-endpoint

But your dialplan that governs calls is:

Context:
sippy-media-anchor

and outbound uses

Dial(SIP/sippy/...)

which is chan_sip.

If Sippy is delivering to PJSIP while governance expects chan_sip (or vice versa), you'll get exactly this kind of mismatch.

Possibility 3

The INVITE is being rejected by the first dialplan step.

For example because of:

From:
Request-URI
Context
Codec
Authentication

before it ever reaches

_2060X.
I think we are now very close.

There is one command that will answer everything in one shot.

Inside the Asterisk CLI

Run:

sip set debug on

(not peer, just on)

Then place one OTP call.

We need to see whether the CLI prints:

<--- SIP read --->
INVITE sip:2060880...

or

INVITE sip:880...

or

INVITE sip:2221...

or nothing.

That single INVITE will tell us:

Request URI
Source IP
Called number
Context
Whether Asterisk rejected it
Whether it entered the dialplan
One more thing I noticed

Your deployment guide says:

Current deployment uses DIRECT_SIP and chan_sip on 5160.

But your running system shows:

sip show peer sippy
Addr->IP : 191.101.30.107:5060

and

ss -lunp
*:5060

So the live server does not exactly match the deployment guide.

That isn't necessarily wrong, but it means we should trust the live Asterisk configuration over the document while troubleshooting.

My confidence level now

Based on everything you've shown so far:

✅ Sippy is attempting to use asterisk(in).
✅ Asterisk is alive and reachable.
✅ SIP OPTIONS succeed.
✅ The _2060X. dialplan is correct.
❌ We have not yet seen a single INVITE.

The missing INVITE is the last missing piece. Once we capture one live INVITE, we should be able to pinpoint the failure within a few minutes.

Review: Ichibaan Asterisk Deployment & Configuration Guide (2026-07-03)

Source: Ichibaan_Asterisk_Deployment_Guide_2026-07-03.docx (BitsAuto Platform, auto-generated). This is a companion reference to the July 2 disk-outage incident report and the ongoing July 3 "calls not passing through" investigation.

What the Guide Documents (Summary)

Server: 159.223.32.59, Ubuntu 20.04 LTS, Asterisk 18.x, manually built from source (not FreePBX, per the guide).

Two Asterisk roles for the BitsAuto platform:


Voice OTP — platform opens a short-lived AMI TCP connection (port 5038), sends Login, then Originate with Application: SayDigits to read OTP digits directly — bypassing the dialplan entirely (per guide).
Call Governance — a persistent AMI connection (reconnects every 15s, Ping every 60s) watches BridgeEnter/Hangup events, matches the vendor-leg channel by regex (e.g. /SIP\/callntalk/), and sends Hangup on just that leg to enforce duration policy.


Channel tech modes (ASTERISK_CHAN_TECH env var, guide states current deployment = DIRECT_SIP):

ModeChannel stringDIRECT_SIP (documented default)SIP/<prefix+number>@191.101.30.107 — direct to Sippy IP, no trunkSIPSIP/TrunkName/number — named chan_sip peerPJSIPPJSIP/number@TrunkName — named PJSIP endpoint

Sippy routing / tech-prefix: SIPPY_TECH_PREFIX (example 22211) is prepended to the destination; Sippy strips the leading 4 digits as a product/routing code. Documented products: 1=First Class, 2=Business Class, 6=Bravo Special, 7=Special Charlie.

Key config file locations (per guide): /etc/asterisk/manager.conf (AMI, user bitsauto), /etc/asterisk/sip.conf (chan_sip, bindport = 5160, non-standard to avoid clashing with Sippy's 5060), /etc/asterisk/extensions.conf ([default], [from-sippy], [bitsauto-otp] contexts).

Guide's own troubleshooting section (12.1–12.5) covers: AMI connection refused, AMI auth failed, OTP call never answers (reasons 1/5), governance events not firing, recordings missing — each with specific CLI commands.

⚠️ Discrepancies vs. What We Actually Found on the Live Server

The guide describes a hand-built vanilla Asterisk install. Everything we've observed on the live server this week points to a FreePBX-managed install instead — a materially different stack from what's documented:

Guide saysLive server actually showsManually built Asterisk, no FreePBXfwconsole, FreePBX admin panel, FreePBX MariaDB-backed config all present and in active use (confirmed throughout the July 2 incident)chan_sip on port 5160Live trunk is a PJSIP endpoint (sippy-endpoint, confirmed via pjsip show endpoints) — not confirmed whether chan_sip/port 5160 is even loaded"Current deployment uses DIRECT_SIP" (§7.1) — dials SIP/number@191.101.30.107 directly, no named trunkLive custom dialplan (extensions_custom.conf, context sippy-media-anchor) dials SIP/sippy/22211<destination> — named-peer syntax, which is the guide's "SIP mode," not DIRECT_SIPInbound Sippy context is [from-sippy]Live context is sippy-media-anchor — different name, custom-written, not the guide's sample dialplanOTP calls bypass the dialplan entirely (Application: SayDigits sent directly in the Originate action, §8.1 note)Live dialplan does have an OTP path: pattern _1. → NoOp(OTP call...), Answer(), StopMixMonitor(), Goto(otp-playback,s,1) — meaning OTP calls currently do enter a context, contradicting the guideGovernance dialplan pattern isn't described in the guide at allLive: pattern _2060X. extracts a product digit and destination, runs MixMonitor, then Dial(SIP/sippy/22211${EXTEN:4},3600,Tt) — this exists only in extensions_custom.conf, undocumented in the guide

None of this is necessarily wrong — the platform may have evolved since the guide was written (or the guide may describe an earlier/parallel design that was later customized). But it means the guide cannot be trusted as an accurate picture of current call routing, and troubleshooting should verify against the live dialplan/config, not the document, when they conflict.

Relevant to the Open "Calls Not Passing Through" Issue

A few things from the guide are directly actionable for that investigation and haven't been checked yet:


Confirm the actual ASTERISK_CHAN_TECH value configured on the platform side (Replit Secrets) — if it's still DIRECT_SIP per the guide's documented default, but the live dialplan is clearly using named-peer SIP/sippy/... syntax (SIP mode behavior), there may be a mismatch between what the platform thinks it's dialing and what the server-side dialplan is actually built to handle. This alone could explain calls silently failing to route.
Guide §12.3 troubleshooting steps for "Voice OTP Call Never Answers" — not yet run on the live server:


bash   asterisk -rx "sip set debug on"      # attempt a call, check SIP INVITE logs
   asterisk -rx "sip show settings"     # confirm bind port — guide says 5160, unconfirmed live
   asterisk -rx "sip show peers"        # confirm chan_sip "Sippy" peer even exists


Guide §12.4 troubleshooting for Call Governance events not firing — check platform logs for [ami-governance] Connected / Logged in; if [ami-governance] AMI not configured appears, ASTERISK_AMI_SECRET isn't set on the platform side.
manager.conf's permit = <PLATFORM_IP>/255.255.255.255 rule — if the BitsAuto/Replit platform's outbound IP has changed since this was configured, AMI would silently reject the platform's own connection attempts the same way it's been rejecting the brute-force attempts from 45.61.188.18 we saw in yesterday's logs. Worth confirming the permitted IP still matches the platform's current egress IP:


bash   cat /etc/asterisk/manager.conf   # check the [bitsauto] permit line


SIPPY_TECH_PREFIX / SIPPY_CLI correctness — guide explicitly warns "wrong prefix causes Sippy to reject routing" and CLI must match "the IP auth rule on Sippy." Given the live dialplan hardcodes 22211 as the prefix (matching the guide's example), if Sippy's IP-auth rule for this CLI was changed on Sippy's side (SB-1), calls could be silently rejected upstream — outside Asterisk's own logs, which would explain why yesterday's log tail showed zero call activity at all.


Security Note

This guide contains a caution banner marking it confidential (server credentials, IPs, config secrets). Noting for the record: AMI (port 5038) is bound to 0.0.0.0 per the documented manager.conf, with the guide's own caution flagging this as a brute-force risk if not restricted to the platform IP — consistent with the repeated AMI brute-force attempts observed in /var/log/asterisk/full from 45.61.188.18 on July 3. Worth confirming in Section 3.3/6.1 whether bindaddr and the firewall rule have actually been restricted, since the guide flags this as a known open risk, not just a hypothetical one.

This is a good review overall. I agree with many of the observations, but there are a few places where the conclusions go beyond what the evidence actually proves.

What I agree with
1. The deployment guide is not an exact representation of production today

This is clearly true.

The guide describes one architecture, while your live server has:

FreePBX managing the dialplan
extensions_custom.conf
sippy-media-anchor
custom governance logic
custom _2060X. routing
custom _1. OTP context

So the production system has evolved beyond the guide. The guide should be treated as design documentation, not as the authoritative source for today's routing.

2. The live dialplan should be trusted over the guide

Completely agree.

You've already shown:

Context: sippy-media-anchor

_2060X.
    MixMonitor(...)
    Dial(SIP/sippy/22211${EXTEN:4},3600,Tt)

That is the code Asterisk is actually executing.

3. The guide does not document Call Governance

Correct.

The governance implementation is custom.

The guide documents the AMI listener architecture but not your actual governance dialplan.

Where I disagree
The reviewer says:

Current deployment uses DIRECT_SIP

That is written in the guide.

But...

That does not prove the running system is using DIRECT_SIP.

In fact you've already shown the opposite.

Your dialplan literally contains

Dial(SIP/sippy/22211${EXTEN:4})

That is a named SIP peer.

So production is using SIP peer mode for this path.

The guide is simply outdated here.

The reviewer says:

There may be a mismatch between ASTERISK_CHAN_TECH and the dialplan.

Possible.

But this has not been demonstrated.

Remember:

Voice OTP and Governance are two separate flows.

The guide itself explains this separation:

Voice OTP originates via AMI
Governance handles inbound calls from Sippy

Those are different call paths.

So even if ASTERISK_CHAN_TECH were wrong, that would explain Voice OTP failures, but not automatically explain why governance isn't seeing calls.

The reviewer says:

AMI permit may reject Replit

Possible.

But you've already shown evidence that the platform connects.

Earlier:

governance connected
dashboard retrieved data
AMI commands succeeded

So AMI authentication is not currently the primary suspect.

The most important new evidence

The biggest development today is actually not the deployment guide.

It is the Sippy reports.

You showed:

Vendor CDR

Vendor
asterisk(in)

Connection
asterisk-in

Remote IP
159.223.32.59

Calls
400

That is strong evidence that Sippy is attempting to send traffic to your Asterisk.

This changes the investigation substantially.

Yesterday the working hypothesis was:

Sippy bypassed Asterisk.

Today's evidence says:

Sippy is selecting the asterisk-in connection.

Those are different.

What still doesn't add up

We now have four facts:

Fact 1

Sippy Vendor CDR shows

Vendor:
asterisk(in)

✔ proven

Fact 2

Remote IP

159.223.32.59

✔ proven

Fact 3

Asterisk listens on SIP

5060/UDP

✔ proven

Fact 4

Asterisk shows

0 active channels
17 calls processed

This does not match Facts 1–3.

That narrows the possibilities

At this point there are only a few realistic explanations:

SIP INVITEs are reaching Asterisk but being rejected before channel creation.
The INVITEs are reaching a different SIP stack/context than expected.
The Vendor CDR records attempted routing rather than successful SIP delivery.
Calls are timing out before Asterisk ever creates a bridged channel.

Your Vendor CDR itself supports the fourth possibility:

Result:
Timeout (100)

Duration:
00:00

Billed:
00:00

Those are failed call attempts, not successful bridged calls.

My recommended next step

I would not spend more time comparing the guide to production.

The guide has already served its purpose.

The next investigation should be entirely on live SIP signalling.

Specifically:

Enable SIP debugging (sip set debug on if using chan_sip, or pjsip set logger on if the inbound leg is actually PJSIP).
Place one controlled OTP test call.
Capture:
whether an INVITE arrives,
which port it arrives on,
which context it enters,
whether Asterisk sends a 100 Trying,
whether it rejects the call,
whether it executes _2060X..

That single transaction will answer more than another hour of document comparison.

Overall assessment

The review is valuable because it identifies where the documentation no longer matches production. However, I would adjust two conclusions:

Supported by evidence: the deployment guide is outdated in several areas relative to the live system.
Not yet supported by evidence: that an ASTERISK_CHAN_TECH mismatch or AMI permit configuration is the root cause of the current governance issue.

Given everything you've gathered today, the investigation should now focus on live SIP message flow, not architecture documentation. The remaining gap is determining exactly what happens to an incoming SIP INVITE between Sippy selecting asterisk-in and Asterisk creating (or failing to create) a channel.