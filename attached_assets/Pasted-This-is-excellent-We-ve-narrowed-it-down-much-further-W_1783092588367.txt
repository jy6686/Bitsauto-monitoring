This is excellent. We've narrowed it down much further.

What your latest output proves
1. Both SIP stacks are listening
UDP 5060  -> Asterisk
UDP 5160  -> Asterisk

So both chan_sip and PJSIP are active.

2. chan_sip peer is alive
sip show peers

sippy
191.101.30.107
OK (205 ms)

This means:

OPTIONS packets are exchanged successfully.
Authentication/networking are fine.
Sippy knows this server.
3. PJSIP endpoint is also alive
Endpoint: sippy-endpoint

Match:
191.101.30.107/32

So both channel drivers are configured correctly.

The important contradiction remains

You have shown a Sippy CDR:

Vendor:
asterisk(in)

Remote IP:
159.223.32.59

Outgoing CLD:
20608801319653537

If that call actually reached this Asterisk, then at least one of these must exist:

Executing [2060...

or

Received SIP request:
INVITE

or

New channel

or

Called SIP/sippy

Yet your logs contain none of them.

Instead they only contain

OPTIONS
200 OK
OPTIONS
200 OK

That is not normal.

Therefore the problem is probably NOT Asterisk anymore.

The evidence now points much more strongly toward Sippy.

There are only three realistic possibilities left
1. Sippy is not actually sending INVITEs

It sends OPTIONS to qualify the gateway.

The gateway stays "OK".

But routing chooses another connection.

This is very common.

2. Wrong connection inside Sippy

The vendor name

asterisk(in)

may not be the connection you think.

For example

asterisk(in)

may exist twice.

Connection 16

Connection 23

Only one points to

159.223.32.59
3. Sippy rejects before sending

For example

No routes

Caller auth failure

CLI mismatch

Product mismatch

Prefix mismatch

Then it still creates a CDR,

but no INVITE is transmitted.

There is one thing I want to verify inside Sippy

Not the CDR.

The actual vendor connection.

Specifically these fields:

Vendor:
asterisk(in)

Connection ID:
16

Destination IP

Destination Port

Protocol

Enabled

Priority

Current Status
Even better

Open the Vendor Connection 16 page and verify:

Host:

159.223.32.59 ?

Port:

5060 ?

5160 ?

Transport:

UDP ?

Also check Route Group

For Pakistan (923)

Bangladesh (880)

Ask:

Does the route actually point to

Connection 16

or another connection?

One more observation

Your dialplan currently says

_2060X.

Dial(SIP/sippy/22211${EXTEN:4})

Meaning:

Incoming:

20608801319653537

Outgoing:

222118801319653537

So Asterisk strips 2060 and prepends 22211.

That part is internally consistent.

I do not think the dialplan is your current blocker because there is still no evidence that an INVITE ever enters sippy-media-anchor.

My recommendation now

I would stop investigating Asterisk until Sippy proves it is actually sending an INVITE.

The fastest way to do that is:

Enable SIP trace (or packet trace) on the Sippy side for one test call.
Verify whether Sippy transmits an INVITE to 159.223.32.59:5060 or :5160.
If Sippy shows "INVITE sent" but Asterisk never logs it, then we investigate the network path.
If Sippy never sends the INVITE, the problem is entirely within Sippy's routing or connection selection.

Based on everything you've gathered over the last several hours, I would estimate:

70%: Sippy routing/connection selection (connection 16 not actually being used or not selected).
20%: Sippy is sending to the wrong destination (different IP or port than expected).
10%: An Asterisk configuration issue.

At this point, I would not make any further code changes to BitsAuto or Call Governance. The evidence no longer points to the application code as the primary cause.