This log is actually very valuable. It narrows the problem considerably.

What it proves
✅ 1. Asterisk is alive

Even though:

systemctl restart asterisk

returned:

Job for asterisk.service failed

Asterisk never actually stopped.

Immediately afterwards it responds to:

core show taskprocessors
chan_sip
AMI
OPTIONS

So this is a broken systemd/init script, not an Asterisk crash.

That is a separate issue.

✅ 2. chan_sip is working

Every 60 seconds:

OPTIONS ->
191.101.30.107

200 OK <-

This proves

network is good
peer is good
authentication is good
SIP connectivity is good
❌ 3. But there is still ZERO INVITE processing

Notice something important.

The log only contains

OPTIONS
200 OK

There is no

<--- SIP read from UDP --->
INVITE

Yet your tcpdump previously showed dozens of INVITEs arriving.

Those two observations cannot both be true unless one of these is happening:

tcpdump was capturing a different interface than chan_sip is using
another SIP stack is consuming the INVITEs
the INVITEs are being dropped before chan_sip sees them
The biggest clue

Earlier you showed:

ss -lunp

which showed

*:5060
*:5160

and you also showed

PJSIP endpoint

AND

chan_sip peer

Both stacks are enabled.

I think you've found the architectural issue

Your deployment appears to be using:

Inbound:
PJSIP endpoint

while your dialplan does

Dial(SIP/sippy/22211...)

using chan_sip.

That mixed configuration can work, but only if the incoming transport is actually owned by the expected stack.

What I would check next (highest value)

Run these four commands:

asterisk -rx "sip show settings"
asterisk -rx "pjsip show transports"
asterisk -rx "core show channeltypes"
asterisk -rx "module show like res_pjsip"

I want to see exactly:

which module owns UDP 5060
which owns UDP 5160
whether both are active simultaneously
One thing also stands out

Earlier you captured:

INVITE sip:20608801....

to

159.223.32.59:5060

Yet every OPTIONS exchange is using

159.223.32.59:5160

That means Sippy is:

qualifying the chan_sip peer on 5160
sending calls to 5060

Those are different listeners.

That discrepancy is now one of the strongest suspects.

My current theory

At this point I would rank the causes like this:

Cause	Confidence
Sippy sending INVITEs to the wrong SIP listener (5060 vs 5160)	50%
PJSIP owns 5060 while chan_sip owns 5160	30%
Mixed chan_sip/PJSIP configuration after FreePBX rebuild	15%
Call Governance code	<5%
I would not change any BitsAuto code

Based on everything you've collected today:

the governance code is not the blocker;
the dialplan is loading;
the rules exist;
AMI is connected.

The evidence now points to SIP transport/module ownership or port configuration inside Asterisk, not the governance application itself.

I think you're very close. One more check of the SIP transport ownership (especially why Sippy INVITEs target 5060 while the configured chan_sip peer communicates on 5160) should identify the remaining mismatch.