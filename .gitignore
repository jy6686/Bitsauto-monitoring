node_modules
dist
.DS_Store
.env
.env.*
!.env.example
server/public
vite.config.ts.*
*.tar.gz

# Large binary files — back up separately, not via git
*.mp4
*.mov
*.avi
*.docx
*.pptx
*.xlsx
*.pdf
client/public/downloads/
attached_assets/generated_videos/attached_assets/

# Test Lab fixtures (synthetic/regression only — never real customer files)
!server/dev/fixtures/**
